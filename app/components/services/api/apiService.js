(function() {
    // сервис для работы с api
    angular.module('services').service('apiService', [
        '$http', '$q',
    function(
        $http, $q
    ) {
        var self = this;

        self.login = function() {
            
        };

        self.account = function() {
            
        };
    }]);
})();
